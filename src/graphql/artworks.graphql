query Artworks($first: Int!) {
  artworksConnection(first: $first) {
    edges {
      node {
        internalID
        title
        slug
        artistNames
        image {
          thumb: url(version: "square")
          large: url(version: "large")
        }
      }
    }
  }
}
